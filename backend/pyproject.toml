[project]
name = "backend"
version = "0.1.0"
description = "Backend FastAPI qui résume des mails via Gemini."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "environs>=15.0.1",
    "fastapi>=0.136.3",
    "google-genai>=1.0.0",
    "pydantic>=2.13.4",
    "uvicorn>=0.49.0",
]
